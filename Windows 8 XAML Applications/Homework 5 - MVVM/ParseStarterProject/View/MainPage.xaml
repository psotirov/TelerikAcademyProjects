<Page
    x:Class="ParseStarterProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ParseStarterProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding Main, Source={StaticResource Locator}}"
    >

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Text="Hmm, I don't know exactly what we have to add to this project. By the way the code is a little bit complicated for me :)" />
        <TextBlock Text="Login"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Style="{StaticResource PageHeaderTextStyle}" />
        <WebView x:Name="webView" Height="400" Width="600"/>
    </Grid>
</Page>
